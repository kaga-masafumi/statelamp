{
  "name": "@statelamp/codex-mcp",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "description": "Local stdio MCP adapter for StateLamp Human Call",
  "main": "server.js",
  "bin": {
    "statelamp-codex-mcp": "./server.js"
  },
  "scripts": {
    "build": "esbuild server.js --bundle --minify --platform=node --format=esm --outfile=dist/server.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.30.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "esbuild": "0.25.9"
  }
}
